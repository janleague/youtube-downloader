# Tauri arayüzü

Bu klasör projenin çalışan React + Tauri masaüstü uygulamasıdır.

```powershell
npm install
npm run tauri dev
```

Python indirme motoru kök dizindeki `backend.py` üzerinden çalışır. Ayrıntılı
kurulum ve test komutları için projenin kök `README.md` dosyasına bakın.
